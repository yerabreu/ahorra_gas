import 'package:flutter/material.dart';

class MapApp extends StatelessWidget {
  const MapApp({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child:  Text("El mapa"));
  }
}